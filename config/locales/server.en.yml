en:
  site_settings:
    rank_colors: "Rank Colors"
    plugins:
      group_colors_enabled: "Enable group color system"
      group_colors_hover_enabled: "Enable hover cards for group colors"
      group_colors_priority_enabled: "Enable priority system for group colors (lower rank takes precedence)"
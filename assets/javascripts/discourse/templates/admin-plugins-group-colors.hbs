{{!-- assets/javascripts/discourse/templates/admin-plugins-group-colors.hbs --}}
<div class="group-colors-admin">
  <h2>{{i18n "group_colors.title"}}</h2>

  <div class="settings-header">
    <button class="btn" {{on "click" this.saveOrder}}>{{i18n "group_colors.save_order"}}</button>
  </div>

  <div class="groups-list sortable">
    {{#each this.sortedGroups as |group|}}
      <div class="group-row sortable-group" data-group-id={{group.id}}>
        <div class="group-sort-handle">{{d-icon "bars"}}</div>
        <div class="group-name">{{group.name}}</div>
        <div class="group-color">
          <ColorInput @value={{group.custom_fields.color}} @onChange={{fn this.updateGroupColor group}} />
        </div>
        <div class="group-preview">
          <span style={{html-safe (concat "color: " group.custom_fields.color)}}>{{group.name}}</span>
        </div>
        <div class="group-rank">{{group.custom_fields.color_rank}}</div>
      </div>
    {{/each}}
  </div>
</div>

